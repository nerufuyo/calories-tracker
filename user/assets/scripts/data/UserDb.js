// const UserDb = {
  // getUser()
  // setGoal(calories, date)
  // updateProfile( { username, email, name, gender, birthday, height, weight, profile_pic, activity } )
  // updatePass( { password } )

  /* belakangan kerjainnya */
  // setNotification(time) 
// }

// export default userDb;